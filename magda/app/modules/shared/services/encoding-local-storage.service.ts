import {Injectable, OnInit} from '@angular/core';
import {LocalStorage} from "../enums/local-storage-coding.enum";

@Injectable({
  providedIn: 'root'
})
export class EncodingLocalStorageService {
/*  localStorageCoding :typeof LocalStorage=LocalStorage;
  userName:string;
  role:string;
  admin:string;
  token:string;

  constructor() { }

  ngOnInit(): void {
    this.userName=localStorage.getItem(this.localStorageCoding.userName);
    this.role=localStorage.getItem(this.localStorageCoding.role);
    this.admin=localStorage.getItem(this.localStorageCoding.admin);
    this.token=localStorage.getItem(this.localStorageCoding.token);
  }*/
}
